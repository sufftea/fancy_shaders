name: some_shaders
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_shaders: ^0.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  shaders:
    - assets/shaders/diagonal_lines.frag
    - assets/shaders/grain.frag
    - assets/shaders/faded_grain.frag
    - assets/shaders/glass.frag
    - assets/shaders/glass_rect.frag
